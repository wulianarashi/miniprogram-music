<scroll-view class="lyric-box" scroll-top="{{scrollTop}}" scroll-y scroll-with-animation="true">
  <view class="lyric-panel">
    <block wx:for="{{lrcList}}" wx:key="*this">
      <view class="lyric {{index == lrcIndex ? 'hight':''}}">{{item.lrc}}</view>
    </block>
  </view>
</scroll-view>