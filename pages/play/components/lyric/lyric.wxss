.lyric-box {
  text-align: center;
  position: absolute;
  bottom: 0%;
  transform: translateY(-50%);
  height: 28%;
  color: var(--lightGray);
  font-size: 32rpx;
}

.lyric-panel {
  position: relative;
  top: 50%;
}

.lyric {
  min-height: 64rpx;
}

.hight {
  color: var(--themeColor);
}
