<view class="mv-list">
  <block wx:for="{{mv_copy_list}}" wx:key="vid">
    <view class="mv-item">
      <image src="{{item.coverUrl}}" class="mv-img"></image>
      <view class="mv-name">{{item.title}}</view>
    </view>
  </block>
</view>
